name: Build and Push k3s-images

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

jobs:
  # Placeholder job — no generic images defined yet.
  # Each image has its own dedicated workflow (e.g. moltbook-manager-build.yaml).
  # Add image-specific workflows following that pattern.
  noop:
    name: No generic images to build
    runs-on: ubuntu-latest
    steps:
      - run: echo "No images defined in generic build.yaml — see per-image workflows"
